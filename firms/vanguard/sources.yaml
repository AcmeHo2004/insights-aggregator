# ─────────────────────────────────────────────────────────────────────────────
# Vanguard — investment-insights source definitions
#
# Sibling project to the other firms/ scanners. Coverage aims to be COMPLETE:
# every firm-owned podcast (native RSS) + every distinct written-insight
# section and market-outlook page (via the sitemap_articles adapter, one web
# source per content domain, each listing all relevant include paths).
#
# Only publicly accessible content; adapters send a normal browser UA and do
# NOT bypass auth/paywalls/bot-gating. Per-source failures are isolated.
# ─────────────────────────────────────────────────────────────────────────────

settings:
  enrich_window_days: 60
  max_enrich_per_feed: 8
  llm_model: claude-haiku-4-5
  cluster_window_days: 3
  web_max_new_per_run: 150

# Known firm-owned podcasts whose public RSS feed URL was NOT resolvable during
# recon (only an Apple Podcasts id found). Resolve via the iTunes lookup API
# (itunes.apple.com/lookup?id=<id> -> feedUrl) once network access allows, then
# add as rss sources. Not fabricated here:
#   - Better Vantage by Vanguard  (apple_id: 1834163410)

sources:
  - name: "Vanguard Insights & VEMO (web)"
    firm: "Vanguard"
    business_unit: "Investing Insights"
    method: scrape
    adapter: sitemap_articles
    content_type: article
    url: https://corporate.vanguard.com/content/corporatesite/us/en/corp/articles/
    tier: 1
    notes: "Best-effort. Vanguard written insights + outlooks via public sitemap; per-source failure isolated."
    params:
      sitemap_url: https://corporate.vanguard.com/sitemap.xml
      include:
        - /corp/articles/
        - /corp/vemo/
      exclude:
        - /literature/
        - /forms/
        - /legal/
        - /sign-in
        - /content/

  - name: "Vanguard Advisor Market Perspectives (web)"
    firm: "Vanguard"
    business_unit: "Advisors"
    method: scrape
    adapter: sitemap_articles
    content_type: article
    url: https://advisors.vanguard.com/insights/article/series/market-perspectives
    tier: 1
    notes: "Best-effort. Vanguard written insights + outlooks via public sitemap; per-source failure isolated."
    params:
      sitemap_url: https://advisors.vanguard.com/sitemap.xml
      include:
        - /insights/
      exclude:
        - /literature/
        - /forms/
        - /legal/
        - /sign-in
        - /content/
