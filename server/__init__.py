"""Optional, self-hosted companion API for Insights Aggregator.

NOT part of the GitHub Pages deploy — Pages stays a pure static frontend. Run this
locally (or on your own host) and point the frontend at it to unlock the features a
static site can't do: cross-device sync of read/star/interests, telemetry capture,
data-driven For You weights, and a real digest. If it isn't running, the frontend
silently falls back to its localStorage-only behaviour.
"""
